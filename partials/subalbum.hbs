<img src="{{relative previews.0.urls.small}}" alt="{{relative previews.0.filename}}">

<a href="{{relative url}}">
    <div class="info">
        <h3>{{title}}</h3>
        <div>{{summary}}</div>
        <div>{{{date stats.fromDate}}} - {{{date stats.toDate}}}</div>
    </div>
</a>